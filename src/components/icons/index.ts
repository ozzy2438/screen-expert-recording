import { 
  Monitor, 
  WindowIcon, 
  Mic, 
  Volume2, 
  Play, 
  Pause, 
  Square, 
  Settings 
} from 'lucide-react';

export const Icons = {
  Monitor,
  Window: WindowIcon, // Alias WindowIcon as Window for consistent naming
  Mic,
  Volume2,
  Play,
  Pause,
  Square,
  Settings,
} as const;